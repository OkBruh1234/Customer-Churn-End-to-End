# Training, EDA and notebook dependencies.
#
# Deliberately NOT installed by either deployment. These are only needed to
# re-run Customer-Churn-End-to-End.ipynb and regenerate churn_model.pkl.
#
#   pip install -r requirements-dev.txt

-r requirements.txt

matplotlib~=3.10
seaborn~=0.13
shap~=0.48
xgboost~=3.1
jupyter~=1.1
